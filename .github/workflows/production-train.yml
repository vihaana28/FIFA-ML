name: Production Train

on:
  schedule:
    - cron: "17 8 * * *"
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger production training
        env:
          PROD_URL: ${{ secrets.PROD_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          test -n "$PROD_URL"
          test -n "$CRON_SECRET"
          curl --fail-with-body --show-error --silent --max-time 240 \
            --request POST \
            --header "Authorization: Bearer $CRON_SECRET" \
            "${PROD_URL%/}/admin/cron/train"
